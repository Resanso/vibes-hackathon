# Shared skills

Custom Claude Code skills/workflows that apply across sub-projects go here (e.g. a
shared release checklist, a cross-platform release-notes generator). Nothing here yet —
this is a placeholder so the directory exists and is tracked by git.

Sub-project-specific skills should live inside that sub-project instead, not here.
